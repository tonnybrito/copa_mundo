<%= form_with(model: phase, local: true) do |form| %>
  <% if phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phase.errors.count, "error") %> prohibited this phase from being saved:</h2>

      <ul>
      <% phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :phase_year %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :phase_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
